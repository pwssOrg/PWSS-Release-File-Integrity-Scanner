# PostgreSQL Config
The database server certificates with default credentials can be found in this folder. It´s all localhost so no worries :)

In addition, it has the PostgreSQL configuration file that allows for SSL communication between the database and Spring.
